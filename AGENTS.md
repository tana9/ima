# エージェント向け作業方針

このファイルはリポジトリ全体に適用します。

## プロジェクト

- Google Apps Scriptで動作する、作業をGoogleカレンダーに記録するWebアプリです。
- アプリのソースは `src/`、Node.jsのテストは `tests/` にあります。
- 利用方法と構成は `README.md`、操作コマンドは `Taskfile.yml` を参照してください。

## 言語

- 説明、コメント、画面の文言は日本語で記述してください。
- テスト名、入力・期待値に使う文言、モックのエラーメッセージも日本語にしてください。
- API名、コードの識別子、日時形式など、仕様上の表記は維持してください。

## 日時の仕様

- 開始時刻は現在時刻を5分単位で切り捨てます。
- 終了時刻の既定値は現在時刻を5分単位で切り上げます。
- 5分の境界に一致する時刻は、そのまま使用します。
- 手動で指定した終了日時は丸めず、開始日時より後であることを検証します。
- 新しい作業の開始時に前の作業を自動終了します。前の開始日時以下になる場合は最低1分の長さを確保します。
- 開始時に前の作業の終了日時を指定した場合は丸めず、前の開始日時より後かつ新しい開始時刻以前であることを検証します。
- 日付・月・年をまたぐ計算でも日付を保持してください。
- 日時の丸め・期間検証は `src/DateValidation.gs` の `DateRules` に集約し、本番とプレビューに同じ処理を使用してください。

## 操作と検証

- テストやデプロイなどの操作は `Taskfile.yml` に集約し、既存のタスクを使用してください。
- コードやテスト、実行手順を変更した場合は `task test` で検証してください。
- 文書だけの変更では、記載内容と差分を確認してください。
- Taskfileを変更した場合は、タスク一覧と対象タスクのドライランも確認してください。
- 画面操作やプレビューの動作を変更した場合は `task test:browser` も実行してください。初回準備は `task setup:browser`、全テストは `task test:all` です。
- デプロイやApps Scriptへのソース反映は、ユーザーから明示的に依頼された場合に実行してください。同じ作業内ですでに許可された操作について再確認は不要です。
- 通常のデプロイ更新には `task deploy DEPLOYMENT_ID=対象ID` を使用し、既存の公開URLを維持してください。新規作成には `task deploy:new` を使用します。

## ドキュメント

- 利用方法や操作手順を変更した場合は `README.md` も更新してください。
- エージェント向けの作業方針はこのファイルに、利用者向けの説明は `README.md` に記載してください。
